<%= form_with(model: yari, local: true) do |form| %>
  <% if yari.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(yari.errors.count, "error") %> prohibited this yari from being saved:</h2>

      <ul>
      <% yari.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :term %>
    <%= form.date_select :term %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
